{% if type == 'footerMenu' %}
  {% include '@twig/menu/footer-menu/footer-menu.html.twig' with {
    links: links
  } only %}
{% elseif type == 'mainMenu' %}
  {% include '@twig/menu/main-menu/main-menu.html.twig' with {
    links: links,
    is_front: is_front
  } only %}
{% endif %}
