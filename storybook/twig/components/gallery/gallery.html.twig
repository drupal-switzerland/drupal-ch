<div {{ componentId ? 'id=' ~ (componentId | clean_class) : '' }} class="gallery">
  {% for paragraph in paragraphs %}
    {% set paragraph = paragraph.entity %}
    {% include '@twig/components/components.html.twig' with {
      type: paragraph.type,
      component: paragraph
    } only %}
  {% endfor %}
</div>
