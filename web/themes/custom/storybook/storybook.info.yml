name: Storybook
type: theme
base theme: stable
description: 'Base theme for using storybook components.'
package: Core
version: VERSION
core: 8.x
core_version_requirement: ^8 || ^9

regions:
  content: 'Content'

component-libraries:
  twig:
    paths:
      - ../../../../storybook/twig

libraries:
  - storybook/twig
